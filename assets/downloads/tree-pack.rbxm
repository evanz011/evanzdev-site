PLACEHOLDER — this is NOT a real Roblox model file.

Roblox Studio cannot open this. It exists so the test checkout on
evanzdev.shop has something to hand over.

Replace it with your real export:
  Studio > right-click your model > Save to File... > tree-pack.rbxm
and overwrite this file, keeping the same name.
